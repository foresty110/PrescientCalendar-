/**
 * LLM 동작 sanity check — `pnpm sanity` 로 실행.
 * 3~5개 시나리오를 직접 호출해서 통과/실패만 콘솔에 출력.
 * docs/scenarios.md 참조.
 */

// TODO: Step 4 LLM 레이어 구현 후 실제 시나리오 추가.
// async function runScenario(name: string, input: string, expect: ...) { ... }

async function main() {
  console.log("sanity: placeholder — Step 4 LLM 레이어 이후 시나리오 추가");
}

void main();
